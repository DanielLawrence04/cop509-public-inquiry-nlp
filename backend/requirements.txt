fastapi>=0.111
uvicorn[standard]>=0.29
python-multipart>=0.0.9

numpy>=1.26
pandas>=2.1
scikit-learn>=1.4
PyMuPDF>=1.24
pytesseract>=0.3.10
pillow>=10.0

# Semantic / hybrid passage retrieval (Notebook 1 + Search tab).
# torch CPU-only wheel is pinned in the Dockerfile via --extra-index-url so the
# Render image stays under the free-tier size budget; sentence-transformers
# brings the MiniLM bi-encoder used by build_embeddings / semantic_search /
# hybrid_search in src/search.py.
sentence-transformers>=2.7,<6
huggingface-hub>=0.20